{
  "name": "cognitia-cloud",
  "version": "0.0.0",
  "private": true,
  "description": "AI GTM workforce platform — production-shaped MVP (Mira, Echo, Atlas, Beacon).",
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc -p tsconfig.json --noEmit && pnpm --filter @cognitia/web run typecheck",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "audit:prod": "pnpm audit --prod --audit-level=high",
    "evidence:gen": "node --experimental-strip-types scripts/gen-evidence-pack.ts && prettier --write docs/security/evidence-pack.json",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md,yaml,yml}\"",
    "lint": "pnpm run typecheck",
    "check": "pnpm run format:check && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.9",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
