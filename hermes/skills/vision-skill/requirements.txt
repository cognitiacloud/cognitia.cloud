# Hermes vision skill — minimal runtime deps.
# Vision-provider SDKs are intentionally NOT listed; each provider is called
# via its public HTTPS API using stdlib urllib so the skill stays light and
# install failures don't block the rest of Hermes.

Pillow>=10.0.0
pytesseract>=0.3.10

# Optional — only needed if you run the MCP stdio server:
#   pip install mcp

# Optional native deps (install via system package manager):
#   tesseract-ocr   (required for OCR / privacy_scan)
#   ffmpeg          (required for video_frame_qc with --video)
