# Episode 002 — upgraded style frames (APPROVAL ONLY)

5 full-resolution 1080×1920 PNG approval frames. **No MP4 rendered. Not
publish-ready.** Premium AI-explainer direction: real proof typeset crisply,
wrapped in glass cards, glows, connection lines, particles, and light sweeps —
Cognitia palette (navy `#070B14` · cyan `#00E5FF` · blue `#58A6FF` · violet
`#7C5CF7` · green `#22C55E` pass-only).

Rendered offline with `render.py` + `frames.py` (Python/PIL + numpy, 2×
supersampled). Re-render: `python3 frames.py [A B C D E]`.

> Honesty note: tool monograms (C/11/HG/FF/QC/TG) and the frame thumbnails are
> **stylized placeholders** to be swapped for real captured screenshots at build
> time. All text values (durations, sizes, QC scores) mirror the planned `run41`
> manifest shape — no fake metrics, no fake app chrome.

---

## Frame A — Hook (`A_hook.png`)
- **Scene purpose:** scroll-stopping cinematic claim that already hints the machine is real.
- **Real proof visible:** two dim, blurred workflow panels floating behind the headline (`$ hermes run --ep 002`, `✓ vo.mp3 00:58`, `qc: PASS 0.91`) + a "real run, not a mockup" chip. Operator avatar bubble, corner.
- **Density:** ~40% (hook breathes; never empty black — layered glows, particles, floating panels).
- **Phone readability:** headline 94px, sub 40px, kicker 24px, caption 52px — all well above the 28px floor; high contrast white on dark with shadow.
- **Animates later (0–2s):** glow/sparkle reveal of background; headline lines mask-rise staggered; "AI video pipeline." cyan glow blooms; avatar bubble scales in; caption karaoke begins.

## Frame B — Pipeline (`B_pipeline.png`)
- **Scene purpose:** "here is the actual engine," 1:1 with the real Hermes pipeline.
- **Real proof visible:** 6 stages with real output names (`script.md 1.4kB`, `vo.mp3 00:58`, `avatar.mp4 chest-up`, `compose.mp4 22.6MB`, `brand 0.91 · risk 0.07`, `ep002.mp4 sent`); QC node flagged as the amber GATE; `~78s end-to-end` metric.
- **Density:** ~62% (full-width glass cards + glowing spine + icon tokens).
- **Phone readability:** tool names 40px, functions 28px, filename chips 24px, all legible; glowing spine guides the eye top→bottom.
- **Animates later (2–8s):** light travels down the cyan spine; each node lights + scales in on arrival; pulse dots fire at each junction; the QC gate pulses amber to foreshadow Frame D.

## Frame C — Build proof (`C_build.png`)
- **Scene purpose:** undeniable receipts — the densest "real operator" beat.
- **Real proof visible:** hero terminal window (traffic-light title bar) with the real run log — `hermes run --episode 002 --proof`, four green `✓` rows with durations/sizes, live `→ qc gate scanning 1800 frames`; cyan callout ring on the HeyGen row (`no crop · chest-up`); 4 artifact cards with sizes + green status dots.
- **Density:** ~72%.
- **Phone readability:** terminal mono 30px (above 28px floor), artifact names 28px, sizes 26px green; highlighted active command line for focus.
- **Animates later (8–20s):** terminal types per-char; each `✓` pops; artifact cards slide up as their row completes; callout ring draws + chip snaps in; subtle particle drift.

## Frame D — QC gate (`D_qc.png`)
- **Scene purpose:** make the QC gate feel powerful — block → fix → pass.
- **Real proof visible:** frame thumbnail with a cyan scan beam (`frame 0942/1800`, SCANNING); red **BLOCKED** card (`✗ avatar crop chin 0.94`, `✗ slide 2 ink 6%`) → arrow → green **PASSED** card (`re-framed chest-up`, `ink 38%`, `fake-AI 0.07`, `brand 0.91`); 4-up checklist of real `vision-skill` thresholds.
- **Density:** ~74%.
- **Phone readability:** card headers 26px, mono detail 24–28px, big checklist values 34px green; red/green semantics instantly readable.
- **Animates later (20–35s):** scan beam sweeps the thumbnail; BLOCKED card shakes + stamps; arrow draws; BLOCKED morphs/crossfades into PASSED with a green glow burst; checklist values count up and check in.

## Frame E — Result / CTA (`E_result.png`)
- **Scene purpose:** satisfying, premium close + delivery proof + CTA.
- **Real proof visible:** Telegram-style delivery card (`ep002.mp4 · 22.6 MB · 1080×1920 · 60s · delivered 11:48` with green ✓✓); operator avatar bubble; wordmark lockup. No fake subscriber counts.
- **Density:** ~60%.
- **Phone readability:** CTA headline 96px, delivery mono 32px, CTA pill 34px on solid cyan (max contrast), wordmark 34px tracked.
- **Animates later (55–60s):** delivery card slides in with ✓✓ pop; headline settles with cyan bloom; CTA pill pulses once; avatar bubble + wordmark fade up; clean hold.

---

## Acceptance recap (these stills already target it)
- ≥3 layers per frame (glow field + proof object + annotation) — no flat-black, no text-only card.
- One accent (cyan) + supporting blue/violet; green/red strictly for pass/fail.
- All on-screen text ≥28px (terminal 30px, captions 52px).
- Avatar only as a framed bubble — never full-screen.
- Every value traces to the planned real `run41` artifacts; placeholders flagged for real-screenshot swap.

**Next step on your approval:** lock these as the visual standard, swap stylized
thumbnails for real captured screenshots, then commission the preview-only
Hermes pass (3 frames + contact sheet + 8–12s clip) — still no final render.
