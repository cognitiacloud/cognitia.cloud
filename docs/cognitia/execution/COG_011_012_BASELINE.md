# COG-011/012 — Baseline

Date: 2026-06-12. Branch `claude/cog-011-012-lead-detail-tenant-provisioning`
from the COG-011 tip `d8dd427` (which sits on `main` @ `99e2627` — main
contains the merged v1.1 stack + doctrine patch A1, `verified_fact`).

| Step                   | Result (verified_fact)                 |
| ---------------------- | -------------------------------------- |
| `pnpm install`         | OK                                     |
| `pnpm check` (pre-012) | 401/401 tests green (COG-011 included) |

COG-011 also exists standalone as PR #44 (CI green); this combined branch
carries it plus COG-012, per the mission brief.
