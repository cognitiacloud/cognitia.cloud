# 65P-5 — CRM Fake Adapter Evidence V3

The adapter remains a local dry-run shape only.

Retained boundaries:

- `live_crm=false`;
- `network_attempted=false`;
- no SDK imports;
- no HTTP transport;
- fake/reserved payloads only;
- tenant scope enforced;
- deterministic local receipt binding required.

Examples: `CRM_FAKE_ADAPTER_EXAMPLES_V3.json`.
