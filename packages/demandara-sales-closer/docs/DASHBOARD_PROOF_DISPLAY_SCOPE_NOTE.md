# F65-2 — Dashboard Proof Display Scope Note

The `operator-dashboard-v3.html` proof strings such as `proof-local-bound-bw-demo-001` are **display-only local fixture proof labels**.

They are not:

- real signatures;
- reviewer identities;
- production proof;
- cryptographic receipts;
- tamper-proof or adversarial proof.

They are retained only as cosmetic static demo labels so the founder/operator can narrate where a local proof path would appear. The real future fix for 65/70 requires signed reviewer approval, reviewer identity/auth, nonce/replay controls, revocation, and a durable audit ledger.

Boundary: local-only, fake/reserved data, fake CRM, no live CRM, no outreach, no sandbox readiness, no controlled-live readiness, no production readiness, no Alta parity claim.
