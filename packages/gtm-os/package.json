{
  "name": "@cognitia/gtm-os",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Proof-Governed GTM OS v0 — mock-only substrate (state machine, append-only ledger, proof receipts, compliance gate, human approval, idempotent mock adapters).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "demo": "vitest run --root ../.. packages/gtm-os/src/demo/runHappyPath.test.ts"
  }
}
