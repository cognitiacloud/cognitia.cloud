# n8n workflow exports

Placeholder JSON exports — import into n8n and wire the nodes. Each file's
contract (trigger, payload, internal endpoints called) is defined in
`../src/index.ts` (`WORKFLOWS`). See `docs/n8n-workflows.md` for the policy on
what may and may not live in n8n.
