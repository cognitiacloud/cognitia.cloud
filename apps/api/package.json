{
  "name": "@cognitia/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cognitia API + pilot proof harness (dev/simulation only)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types \"src/**/*.test.ts\"",
    "check": "pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0"
  }
}
